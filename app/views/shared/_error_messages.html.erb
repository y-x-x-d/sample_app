<% if @user.errors.any? %>
<!-- any = 配列に１個でも入ってるかどうかを調べる。errorsの中に１個以上あればtrue、何もないはfalse -->
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@user.errors.count, "error") %>.
    </div>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>